# Scweet v4 example .env (placeholders only).
#
# Usage:
#   from Scweet import Scweet
#   scweet = Scweet.from_sources(env_path="examples/.env", provision_on_init=True)
#
# Notes:
# - Keys are case-insensitive.
# - this is not a recommended approach (it only allows using one account). I kept it only for v3 backward compatibility

# Identity (optional; Scweet can derive a synthetic username if missing)
USERNAME=your_username_here
EMAIL=you@example.com

# Credentials (used only for optional nodriver bootstrap)
PASSWORD=your_password_here
EMAIL_PASSWORD=your_email_password_here
TWO_FA=YOUR_TOTP_SECRET

# API cookies (recommended if you already have them)
AUTH_TOKEN=YOUR_AUTH_TOKEN
CT0=YOUR_CT0

# Optional proxy for this account (used for API calls + nodriver bootstrap)
# URL form (supports user:pass@):
PROXY=http://user:pass@host:port
# JSON dict form:
# PROXY={"host":"127.0.0.1","port":8080,"username":"proxyuser","password":"proxypass"}
